{% if env.getConfig("rename_club_enabled") and env.user.clubId == env.getRequestParameter("id") %}
<p class="mb-3"><a href="{{ env.getInternalUrl("rename-club") }}" class="btn btn-sm btn-outline-primary">
	<i class="bi bi-pencil"></i> {{ i18n.getMessage("rename-club_navlabel") }}
</a></p>
{% endif %}