
{% extends "base.twig" %}

{% block page_title %}
{{ i18n.getMessage("topstrikers_title") }}
{% endblock %}

{% block page_content %}

<form class="d-flex flex-wrap gap-2 align-items-center mb-3" method="post">

	<label class="form-label mb-0" for="leagueid">{{ i18n.getMessage("entity_league") }}</label> 
	<select id="leagueid" name="leagueid" class="form-select select-form-submit">
		<option></option>
	{% for leagueItem in leagues %}
		<option value="{{ leagueItem.league_id }}"{% if env.getRequestParameter("leagueid") == leagueItem.league_id %} selected{% endif %}>{{ leagueItem.league_country }} - {{ leagueItem.league_name }}</option>
	{% endfor %}
	</select>

	<input type="hidden" name="page" value="topstrikers" />
</form>

{% if players is defined and players|length > 0 %}

	<table class="table table-striped">
		<thead>
			<tr>
				<th>#</th>
				<th>{{ i18n.getMessage("playertable_head_name") }}</th>
				<th>{{ i18n.getMessage("entity_player_verein_id") }}</th>
				<th>{{ i18n.getMessage("entity_player_sa_spiele") }}</th>
				<th>{{ i18n.getMessage("entity_player_sa_tore") }}</th>
			</tr>
		</thead>
		<tbody>
		
			{% for player in players %}
			<tr>
				<td><strong>{{ loop.index }}</strong></td>
				<td><p><a href="{{ env.getInternalUrl("player", "id=" ~ player.id) }}">{{ player.firstname }} {% if player.pseudonym|length > 0 %}"{{ player.pseudonym }}" {% endif %}{{ player.lastname }}</a></p></td>
				<td>
				{% if player.team_id > 0 %}
				<a href="{{ env.getInternalUrl("team", "id=" ~ player.team_id) }}">{{ player.team_name }}</a>
				{% else %}
				-
				{% endif %}
				</td>
				<td>{{ player.matches }}</td>
				<td>{{ player.goals }}</td>
			</tr>
			{% endfor %}
		
		</tbody>
	</table>

{% else %}
	<p>{{ i18n.getMessage("no_players_found") }}</p>
{% endif %}

{% endblock %}