{% extends "base.twig" %}

{% block page_title %}
{{ i18n.getMessage("leagues_title") }}
{% endblock %}

{% block page_content %}

<form class="d-flex flex-wrap gap-2 align-items-center mb-3" method="post">

	<label class="form-label" for="country">{{ i18n.getMessage("entity_league_land") }}</label> 
	<select id="country" name="country" class="form-select select-form-submit">
		<option></option>
	{% for country in countries %}
		<option value="{{ country.name }}">{{ country.name }} ({{ country.noOfLeagues }})</option>
	{% endfor %}
	</select>

	<input type="hidden" name="page" value="leagues" />
</form>

<div id="leagues-selection"></div>
{% endblock %}