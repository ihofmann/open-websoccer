{% macro premiumbadge(fee) %}
	<span class="badge bg-danger wstooltip" title="{{ i18n.getMessage("premium_fature_fee_tooltip") }}" style="cursor: default;"><i class="bi bi-key"></i> {{ fee }} {{ env.getConfig("premium_credit_unit") }}</span>
{% endmacro %}
